services:
  systemq-staging-be:
    build:
      context: ./be
      dockerfile: Dockerfile

    image: quantumteknologi/systemq-staging-be:latest
    container_name: systemq-staging-be
    ports:
      - "${PORT:-3000}:3000"
    environment:
      APP_ENV: staging
    restart: always
    volumes:
      - ../../storage:/app/storage

  systemq-staging-fe:
    build:
      context: ./fe
      dockerfile: Dockerfile
      args:
        VITE_APP_ENV: staging
    ports:
      - "${PORT:-5173}:5173"

    image: quantumteknologi/systemq-staging-fe:latest
    container_name: systemq-staging-fe
    restart: always
