name: systemq

services:
  redis:
    image: redis:7-alpine
    container_name: systemq-redis
    ports:
      - "6379:6379"
    volumes:
      - ../.tmp/redis:/data
    command: redis-server --save 60 1 --loglevel warning
    restart: always

networks:
  default:
    name: systemq-network